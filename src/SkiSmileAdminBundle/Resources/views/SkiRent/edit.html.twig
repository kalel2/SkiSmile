{% extends '@SkiSmileAdmin/SkiRent/new.html.twig' %}
{% block ski_form_title %}
    <h3>{{ 'Редагування запису'|trans }}</h3>
{% endblock %}
{% block ski_form_actions %}
    <div class="row">
        <div class="col s12 m3">
            <a class="btn" href="{{ path('ski_rent_admin_index') }}">{{ 'Повернутись'|trans }}</a>
        </div>
        <div class="col s12 m3 submit-button">
            {{ form_start(delete_form) }}
            <input class="btn red" type="submit" value="{{ 'Видалити'|trans }}">
            {{ form_end(delete_form) }}
        </div>
    </div>
{% endblock %}

{{ form_start(form, { 'action': path('contact_home'), 'method': 'POST', 'attr': {'class': 'blogger'}}) }}
{{ form_widget(form) }}
<input type="submit" value="Відправити"/>
{{ form_end(form) }}
{#{{ form_start(form, { 'action': path('BloggerBlogBundle_contact'), 'method': 'POST', 'attr': {'class': 'blogger'} }) }}#}